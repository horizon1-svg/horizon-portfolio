{
  "name": "horizon-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.45.0",
    "sharp": "^0.35.2",
    "vite": "^6.0.0",
    "wrangler": "^4.111.0"
  }
}
